import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.mobileticketsapp',
  appName: 'MobileTicketsIonic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
